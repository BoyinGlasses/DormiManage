<UserControl x:Class="DormitoryManagement.WPF.Views.Settings.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:DormitoryManagement.WPF.Controls">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel>
            <TextBlock Text="User Management" FontSize="24" FontWeight="SemiBold" DockPanel.Dock="Left" />
        </DockPanel>
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" Padding="18" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,18,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="260" Margin="0,0,10,0" />
                <ComboBox Width="160" Margin="0,0,10,0">
                    <ComboBoxItem Content="Admin" />
                    <ComboBoxItem Content="Manager" />
                    <ComboBoxItem Content="Staff" />
                    <ComboBoxItem Content="Student" />
                </ComboBox>
                <Button Content="Refresh" Command="{Binding LoadCommand}" />
            </StackPanel>
        </Border>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Users}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*" />
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="2*" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*" />
                <DataGridTextColumn Header="Role" Binding="{Binding RoleName}" Width="*" />
                <DataGridTemplateColumn Header="Status" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <controls:StatusBadge Status="{Binding Status}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
