<UserControl x:Class="DormitoryManagement.WPF.Views.Students.StudentDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel>
            <TextBlock Text="Student Detail" FontSize="24" FontWeight="SemiBold" DockPanel.Dock="Left" />
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Refresh" />
                <Button Content="Add" />
            </StackPanel>
        </DockPanel>
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" Padding="18" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <TextBlock Text="TODO: Wire this view to its ViewModel and application service." TextWrapping="Wrap" />
        </Border>
    </Grid>
</UserControl>
