<UserControl x:Class="DormitoryManagement.WPF.Views.Billing.InvoiceDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:DormitoryManagement.WPF.Controls">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <controls:PageHeader Title="Invoice Detail" Subtitle="Review charges, payment status, and remaining balance.">
                    <controls:PageHeader.Actions>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Print" Command="{Binding PrintCommand}" Margin="0,0,8,0" />
                            <Button Content="Export" Command="{Binding ExportCommand}" />
                        </StackPanel>
                    </controls:PageHeader.Actions>
                </controls:PageHeader>

                <controls:ErrorState Title="Invoice action failed"
                                     Message="{Binding ErrorMessage}"
                                     Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}"
                                     Margin="0,0,0,14" />

                <Border Background="{StaticResource SuccessLightBrush}"
                        BorderBrush="{StaticResource SuccessBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12"
                        Margin="0,0,0,14"
                        Visibility="{Binding HasActionMessage, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="{Binding ActionMessage}" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold" />
                </Border>

                <Border Style="{StaticResource SectionCardStyle}" Margin="0,0,0,18">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Invoice" Style="{StaticResource LabelText}" />
                            <TextBlock Text="{Binding InvoiceNumber}" Style="{StaticResource HeadingText}" Margin="0,4,0,8" />
                            <TextBlock Text="{Binding BillingPeriod}" Style="{StaticResource SubtitleText}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <controls:StatusBadge Status="{Binding Status}" Margin="0,0,0,10" />
                            <TextBlock Text="Remaining" Style="{StaticResource LabelText}" TextAlignment="Right" />
                            <TextBlock Text="{Binding RemainingText}" Style="{StaticResource HeadingText}" TextAlignment="Right" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="360" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource SectionCardStyle}" MinHeight="420">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="Invoice items" Style="{StaticResource SectionTitle}" />
                            <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" Visibility="{Binding Items, Converter={StaticResource HasItemsToVisibility}}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Fee type" Binding="{Binding FeeType}" Width="*" />
                                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*" />
                                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity, StringFormat=N2}" Width="*" />
                                    <DataGridTextColumn Header="Unit price" Binding="{Binding UnitPrice, StringFormat=N0}" Width="*" />
                                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=N0}" Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                            <controls:EmptyState Grid.Row="1"
                                                 Title="No invoice items"
                                                 Message="Charges will appear here after an invoice is selected or generated."
                                                 Icon="₫"
                                                 Visibility="{Binding Items, Converter={StaticResource EmptyCollectionToVisibility}}" />
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="2">
                        <Border Style="{StaticResource SectionCardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="Summary" Style="{StaticResource SectionTitle}" />
                                <Grid Margin="0,4,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Subtotal" Style="{StaticResource BodyText}" />
                                    <TextBlock Grid.Column="1" Text="{Binding SubtotalText}" />
                                    <TextBlock Grid.Row="1" Text="Discount" Style="{StaticResource BodyText}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DiscountText}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="2" Text="Penalty" Style="{StaticResource BodyText}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PenaltyText}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="3" Text="Paid" Style="{StaticResource BodyText}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding PaidText}" Margin="0,8,0,0" />
                                    <TextBlock Grid.Row="4" Text="Remaining" FontWeight="SemiBold" Margin="0,12,0,0" />
                                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding RemainingText}" FontWeight="SemiBold" Margin="0,12,0,0" />
                                </Grid>
                                <Button Content="Pay" Command="{Binding PayCommand}" Style="{StaticResource PrimaryButtonStyle}" IsEnabled="{Binding CanPay}" Margin="0,18,0,0" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock Text="Adjustment" Style="{StaticResource SectionTitle}" />
                                <TextBlock Text="Amount" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding AdjustmentAmount, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Adjustment amount" />
                                <TextBlock Text="Reason" Style="{StaticResource FieldLabelStyle}" Margin="0,12,0,6" />
                                <TextBox Text="{Binding AdjustmentReason, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Adjustment reason" />
                                <TextBlock Text="{Binding AdjustmentReasonError}"
                                           Style="{StaticResource FieldErrorStyle}"
                                           Visibility="{Binding HasAdjustmentReasonError, Converter={StaticResource BoolToVisibility}}" />
                                <Button Content="Save Adjustment" Command="{Binding AdjustCommand}" Margin="0,14,0,0" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
        <controls:LoadingOverlay Message="Saving invoice..." Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}" />
    </Grid>
</UserControl>
