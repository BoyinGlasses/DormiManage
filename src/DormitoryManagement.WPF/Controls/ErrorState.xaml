<UserControl x:Class="DormitoryManagement.WPF.Controls.ErrorState"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource DangerLightBrush}"
            BorderBrush="{StaticResource DangerBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="14">
        <DockPanel LastChildFill="True">
            <Border DockPanel.Dock="Left" Width="24" Height="24" CornerRadius="12" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource DangerBrush}" BorderThickness="1" Margin="0,0,10,0">
                <TextBlock Text="!" FontWeight="Bold" Foreground="{StaticResource DangerBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel>
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource DangerBrush}" />
                <TextBlock Text="{Binding Message, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           TextWrapping="Wrap"
                           Margin="0,4,0,0" />
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>
