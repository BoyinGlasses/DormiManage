# Merged Findings

## P1

1. Screenshot evidence is not deterministic against the canonical ticket sample because the review test fixture uses different counts/categories/IDs than the HTML ref.

## P2

2. Pixel gate is proportional only because the generated WPF artifact is not viewport-normalized to the canonical PNG.
3. Delete row action lacks destructive hover/affordance parity with the ref.

## P3

4. Unused DataGrid styles remain in the view after the move to the shared-column custom table.
5. Non-default states still mix English copy into a Vietnamese route.
